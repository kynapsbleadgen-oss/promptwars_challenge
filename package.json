{
  "name": "wayfare-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "node backend/src/server.js",
    "dev": "npm run dev --prefix frontend"
  }
}
